import { createRoot } from "react-dom/client";
import App from "./App.jsx";

createRoot(document.getElementById("root")).render(
  // <StrictMode>
  <App />
  // </StrictMode>,
);

// Chess bot engine with some cool effects
// Click to spawn in nonsense that has gravity, can spawn in a bunch
// Random objects, literal PNGs with SVGs
// Theme switcher
// Retro TV with fisheye lens
// Blackjack can appear in dark parts, hovering over him causes him to come out of the shadows
//
